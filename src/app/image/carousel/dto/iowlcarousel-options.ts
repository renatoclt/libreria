/**
 * Interface con las opciones que tenemos en owlCarousel
 */
export interface IOwlcarouselOptions {
    items: number;
    dots: boolean;
    navigation: boolean;
    nav: boolean;
    autoplay: boolean;
    autoplayTimeout: number;
    autoplayHoverPause: boolean;
    loop: true;
    onTranslate: any;
}
